{
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "env-cmd": "^10.1.0",
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "nodemon": "^3.0.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "env-cmd -f dev.env nodemon index.js",
    "build":"npm run build"
  },
  "devDependencies": {
    "dotenv": "^16.3.1"
  }
}
